# Copy this file to .env for local development.
# .env is in .gitignore, so it never gets committed.

# Optional. Leave it commented out and the app stores notes in memory instead.
# DATABASE_URL=postgresql://localhost:5432/myapp

# The platform always uses 8080. Only change this locally if 8080 is taken.
PORT=8080
